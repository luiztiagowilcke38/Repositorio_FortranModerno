# Resolução: Interoperabilidade e Integração de Sistemas

## Exercícios do Livro

    -  Explique a importância do atributo `VALUE` na interoperabilidade com C.
    -  Como lidar com vetores e matrizes (estilo C vs estilo Fortran) na fronteira de interoperabilidade?
    -  Escreva um programa em C que chame uma sub-rotina Fortran que realiza a inversão de uma matriz 3x3.
    -  Utilize o `f2py` para expor o seu solver de Kuramoto (Capítulo 56) para o Python e plote a transição de fase com Matplotlib.
    -  Chame a função `qsort` da biblioteca padrão do C (`stdlib.h`) a partir de um programa Fortran.
    -  Avalie o impacto de performance (sobrecarga) ao passar dados via \textit{Shared Memory} entre Julia e Fortran.
    -  O que é o projeto \textit{LFortran} e como ele planeja revolucionar a interoperabilidade via LLVM?
